// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Indexes } from '../../titan/model/indexes.js';
import { Morphs } from '../../titan/model/morphs.js';
import { Vertices } from '../../titan/model/vertices.js';


export class Buffer {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Buffer {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsBuffer(bb:flatbuffers.ByteBuffer, obj?:Buffer):Buffer {
  return (obj || new Buffer()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsBuffer(bb:flatbuffers.ByteBuffer, obj?:Buffer):Buffer {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Buffer()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

indexBuffer(index: number, obj?:Indexes):Indexes|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new Indexes()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

indexBufferLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

vertexBuffer(index: number, obj?:Vertices):Vertices|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new Vertices()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

vertexBufferLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

morphs(index: number, obj?:Morphs):Morphs|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new Morphs()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

morphsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startBuffer(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addIndexBuffer(builder:flatbuffers.Builder, indexBufferOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, indexBufferOffset, 0);
}

static createIndexBufferVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startIndexBufferVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addVertexBuffer(builder:flatbuffers.Builder, vertexBufferOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, vertexBufferOffset, 0);
}

static createVertexBufferVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startVertexBufferVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addMorphs(builder:flatbuffers.Builder, morphsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, morphsOffset, 0);
}

static createMorphsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startMorphsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endBuffer(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createBuffer(builder:flatbuffers.Builder, indexBufferOffset:flatbuffers.Offset, vertexBufferOffset:flatbuffers.Offset, morphsOffset:flatbuffers.Offset):flatbuffers.Offset {
  Buffer.startBuffer(builder);
  Buffer.addIndexBuffer(builder, indexBufferOffset);
  Buffer.addVertexBuffer(builder, vertexBufferOffset);
  Buffer.addMorphs(builder, morphsOffset);
  return Buffer.endBuffer(builder);
}
}
